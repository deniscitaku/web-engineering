// TODO add validation

/*
const validateName = (value) => {
    if (value.length > 5) {
        return false;
    }

    return true;
}

const validateEmail = (value) => {
    return true;
}


const validateLength = (input, min, max) => {

    let length = input.value.length;
    return length > min && length < max;
}

const getErrorSpan = (input) => {
    let errorSpan;
    while ((errorSpan = input.nextSibling.getElementsByClassName('error')[0]));
    return errorSpan;
}*/
